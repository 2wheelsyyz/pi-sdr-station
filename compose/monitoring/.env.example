# Copy to ".env" (gitignored).
GRAFANA_ADMIN_PASSWORD=change-me

# (APRS data now comes from the WX3in1 telnet interface — host/creds are set in
#  the aprs-exporter service env in docker-compose.yml, defaults admin/admin.)
